# Core agentic stack
langgraph>=0.3.0
langchain-anthropic>=0.3.0
langchain-mcp-adapters>=0.1.0

# RAG fallback (local, offline embeddings + vector store) — see rag.py.
# sentence-transformers pulls in torch, a large (~2GB) download.
langchain-community>=0.3.0
langchain-huggingface>=0.1.0
langchain-text-splitters>=0.3.0
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0

# Config / env
python-dotenv>=1.0.0

# Note: requires Python 3.11+ (see CLAUDE.md). The existing .venv is 3.9 and
# must be recreated with Python 3.11 before installing these.
