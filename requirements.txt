# Core agentic stack
langgraph>=0.3.0
langchain-anthropic>=0.3.0
langchain-mcp-adapters>=0.1.0

# Config / env
python-dotenv>=1.0.0

# Note: requires Python 3.11+ (see CLAUDE.md). The existing .venv is 3.9 and
# must be recreated with Python 3.11 before installing these.
